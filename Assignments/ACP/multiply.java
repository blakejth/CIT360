//File bypassed to a single file does all the math.

package apc;

public class multiply implements operation {

	public Integer numbers(Integer fistNumber, Integer secondNumber) {
		Integer sum = firstNumber * secondNumber;
		return(sum);
	}
}